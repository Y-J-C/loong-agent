'use strict';

// Deprecated: TUI history is rendered from a single source of truth in
// state.messages. Keeping this module inert prevents the old transcript append
// path from being reintroduced accidentally.
module.exports = {};
